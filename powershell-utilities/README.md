# PowerShell Utilities

This folder contains PowerShell utilities and scripts used across the workspace.

Files:

- `merge-pdfs.ps1` — helper script to merge PDF files.

Usage:

Run scripts from PowerShell (Windows) or `pwsh` on other platforms. Example:

```powershell
./merge-pdfs.ps1 -InputFolder . -Output combined.pdf
```
